### Функции которые могут быть использованы в Python скриптах:

- *def on_update() -> void* - функция вызывается постоянно.
- *def get_script_name() -> string* - функция вызывается при загрузке скрипта. Результат который вернет эта функция, будет являться названием скрипта
